import express from "express"
const router = express.Router()

// website
// get entry for which item needs to be added - create here, next func
// get item which has been added but needs update - prev func
// add item with entry and set next entry, prev entry - overwrite product if with the same entry exists 
// get complete bucket
// add to cart
// cancel - remove all the items, set prev and next - dont delete




export default router